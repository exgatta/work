# work — 仕事用プロジェクト置き場

がった（exgatta）の**仕事用**リポジトリ。1プロジェクト = 1フォルダ。
私用のものは `exgatta/private` に置き、ここには入れない（逆も同じ）。

## 最初に必ず読む
@WORK_PROFILE.md

## このリポジトリでのルール
- **ブランチは main 一本**。作業ブランチや PR は作らず、main で作業して main に直接 push する（大きな作り直しで事前確認したい時だけ、指示があれば別ブランチ＋PR）。
- 新しい案件は直下にフォルダを作る（例: `2026-10_セミナー資料/`）。既存フォルダに混ぜない。
- **社外秘・顧客名・上司/同僚の実名をコミットしない**（匿名化: 顧客A社、上司X さん 等）。
  このリポジトリは将来共有する可能性がある前提で扱う。
- 会話で新しい好み・判断パターンが分かったら `WORK_PROFILE.md` を更新してコミットし、「プロフィールを更新しました」と一言報告する。
